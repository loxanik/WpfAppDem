<Window x:Class="WpfApp1.WindowAuth"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Icon="/Icon.ico"
        Title="Авторизация" Height="600" Width="800">

    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="Логин" Margin="10 0"/>
                <TextBox MinWidth="200" Text="{Binding Login}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="Пароль" Margin="10 0"/>
                <TextBox MinWidth="200" Text="{Binding Password}"/>
            </StackPanel>

            <Button Content="Войти" Width="200" Click="Button_Click"/>
            <Button Content="Войти как гость" Width="200" Name="GuestLogin" Click="GuestLogin_Click"/>


        </StackPanel>
    </Grid>

</Window>
