<Window x:Class="WpfApp1.WindowEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="WindowEdit" Height="450" Width="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Единица измерения"/>
            <ComboBox ItemsSource="{Binding Units}" 
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding CurrentGood.UnitId}"/>
            <TextBlock Text="Поставщик"/>
            <ComboBox ItemsSource="{Binding Suppliers}" 
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding CurrentGood.SupplierId}"/>
            <TextBlock Text="Производитель"/>
            <ComboBox ItemsSource="{Binding Manufactorers}" 
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding CurrentGood.ManufactorerId}"/>
            <TextBlock Text="Категория"/>
            <ComboBox ItemsSource="{Binding Categories}" 
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding CurrentGood.CategoryId}"/>
            <TextBlock Text="Количество"/>
            <TextBox Text="{Binding CurrentGood.Amount}"/>
            <TextBlock Text="Скидка"/>
            <TextBox Text="{Binding CurrentGood.Discount}"/>
            <TextBlock Text="Описание"/>
            <TextBox Text="{Binding CurrentGood.Description}"/>
            <Button Content="Сохранить"
                    Margin="10"
                    Width="200"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Name="ButtonSave"
                    Click="ButtonSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
